extends layout

block content
    h1= title
    p Welcome to #{title}
    form(action='/signup', method='POST')
        input(
            type = 'text'
            name = 'firstname'
        )
        button(type='submit',
        name = 'GoButton'
        ) Go!


